---
- name: Manage a VIP
  bigip_virtual_server:
    server: "{{ inventory_hostname }}"
    user: "{{ username }}"
    password: "{{ password }}"
    name: "{{ name }}"
    all_rules:
      - "{{ all_rules }}"
    state: "{{ state }}"
  delegate_to: localhost
...
